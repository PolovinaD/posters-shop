image:
  # Override with: --set image.repository=$ECR_REGISTRY/users
  repository: YOUR_ECR_REGISTRY/users
  tag: "latest"
replicaCount: 1

service:
  port: 80
  targetPort: 8000

resources:
  requests:
    cpu: 100m
    memory: 192Mi
  limits:
    cpu: 300m
    memory: 384Mi

envFrom:
  - secretRef:
      name: postershop-db
env:
  - name: SERVICE_NAME
    value: "users"
  - name: LOG_LEVEL
    value: "INFO"
  - name: ROOT_PATH
    value: "/api/users"
  - name: DATABASE_URL
    valueFrom:
      secretKeyRef:
        name: postershop-db
        key: DATABASE_URL_USERS
  - name: JWT_SECRET
    valueFrom:
      secretKeyRef:
        name: postershop-jwt
        key: JWT_SECRET
  - name: CORS_ORIGINS
    value: "http://localhost:3000"

ingress:
  enabled: false

migrations:
  enabled: true
  secretName: postershop-db
  secretKey: DATABASE_URL_USERS

